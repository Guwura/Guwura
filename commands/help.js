const Eris = require("eris");

module.exports.run = (bot, msg, args, tools) => {
    bot.createMessage(msg.channel.id, "i dont rly have a command list rn qwq");
}

module.exports.help = {
    name: "test"
} 
